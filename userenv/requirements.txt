# Hermes dependencies — base Hermes is pip-installed from /opt/hermes in the Dockerfile

# Web framework (not in Hermes)
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9

# Whisper STT — Hermes optional[voice] group, installed explicitly here
faster-whisper>=1.0
numpy>=1.24.0,<3

# MCP SDK — required by Hermes for MCP server connectivity
# Without this, Hermes silently disables all MCP tool support
mcp>=1.0,<2
