# CentralCore-specific additions — Hermes + its deps come from pip install /opt/hermes in Dockerfile

# Web framework (not in Hermes)
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9

# Whisper STT — Hermes optional[voice] group, installed explicitly here
faster-whisper>=1.0
numpy>=1.24.0,<3
