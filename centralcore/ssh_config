# SSH configuration for centralcore / Hermes agent
# Mounted into the container; changes take effect after container restart (no rebuild needed).
# The SSH user and key are injected per-connection from the database (userenv_manager).

Host *
    StrictHostKeyChecking accept-new
    ConnectTimeout 10
